# 🚀 Быстрый старт за 5 минут

## Шаг 1: Установка (2 минуты)

### Вариант A: Автоматическая установка (рекомендуется)

```bash
cd telegram-bot-builder
npm install
npm run install-all
```

### Вариант B: Ручная установка

```bash
# Backend
cd telegram-bot-builder/backend
npm install

# Frontend
cd ../frontend
npm install
```

## Шаг 2: Запуск (1 минута)

### Вариант A: Запуск одной командой

```bash
# Из корневой папки проекта
npm start
```

### Вариант B: Запуск через bash скрипт

```bash
./start.sh
```

### Вариант C: Запуск вручную

Откройте два терминала:

**Терминал 1 - Backend:**
```bash
cd backend
npm start
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm start
```

## Шаг 3: Получение токена бота (1 минута)

1. Откройте Telegram
2. Найдите **@BotFather**
3. Отправьте `/newbot`
4. Введите имя бота (например: `My Test Bot`)
5. Введите username бота (должен заканчиваться на `bot`, например: `my_test_bot`)
6. Скопируйте токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 4: Создание первого бота (1 минута)

1. Откройте браузер: `http://localhost:3000`
2. Нажмите **"Создать бота"**
3. Введите название: `Мой первый бот`
4. Вставьте токен из BotFather
5. Нажмите **"Создать"**
6. Добавьте блок **"Start команда"**
7. Настройте приветствие
8. Нажмите **"Сохранить и запустить"**

## Готово! 🎉

Теперь откройте Telegram и отправьте `/start` вашему боту!

---

## Что дальше?

- Изучите [примеры ботов](EXAMPLES.md)
- Прочитайте [полную документацию](README.md)
- Экспериментируйте с разными блоками!

## Проблемы?

### Порт занят
Если порт 3000 или 5000 занят, измените в коде:
- Backend: `backend/server.js` - переменная `PORT`
- Frontend: создайте `.env` файл с `PORT=3001`

### Не установлен Node.js
Скачайте и установите с [nodejs.org](https://nodejs.org/)

### Бот не отвечает
1. Убедитесь, что backend запущен
2. Проверьте, что бот в статусе "Запущен"
3. Проверьте правильность токена
4. Проверьте консоль на ошибки
